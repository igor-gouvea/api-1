apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-1
  name: api-1-svc
  namespace: lab
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: api-1
  type: ClusterIP
