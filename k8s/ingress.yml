apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: lab
  name: api-1-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: api-1.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-1-svc
            port: 
              number: 8080